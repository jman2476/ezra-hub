info:
  name: Set Subscriptions
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3294/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
