info:
  name: Reset Users Table
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3294/admin/reset
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
